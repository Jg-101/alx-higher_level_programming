#!/usr/bin/node
// prints 3 lines: (like 1-multi_languages.js) but by
// using an arr of str and a loop

const langs = ['C is fun', 'Python is cool', 'Javascript is amazing'];

for (let i = 0; i < 3; i++) {
  console.log(langs[i]);
}
